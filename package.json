{
  "name": "keel-monorepo",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "clean": "node scripts/clean-tmp.js"
  },
  "engines": {
    "node": ">=18"
  }
}
