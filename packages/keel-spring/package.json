{
  "name": "keel-spring",
  "version": "0.1.0",
  "description": "Generador Spring Boot para diseños Keel: instala la skill del generador en el workspace y valida el diseño agnóstico; el código lo genera el agente.",
  "type": "module",
  "bin": {
    "keel-spring": "src/cli.js"
  },
  "scripts": {
    "test": "node --test",
    "compile-check": "node scripts/compile-check.js && node scripts/compile-check.js asset-vault --broker=snssqs && node scripts/compile-check.js notification-mailer --broker=snssqs",
    "broker-check": "node scripts/broker-check.js",
    "mail-check": "node scripts/mail-check.js"
  },
  "files": [
    "src",
    "assets",
    "vendor"
  ],
  "keel": {
    "dsl": "2.13"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "keel",
    "spring-boot",
    "codegen",
    "agents",
    "dsl"
  ],
  "author": "asuridev",
  "license": "MIT",
  "dependencies": {
    "@clack/prompts": "^1.7.0",
    "commander": "^14.0.0",
    "keel-core": "^0.2.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.6.0"
  }
}
